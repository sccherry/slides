---
title: DOM/Events
---

```js
const element = document.querySelector(CSS SELECTOR);
// OR document.querySelectorAll(CSS SELECTOR);

element.addEventListener('click', (event) => {
  // Do something when an event happens.
});
```
